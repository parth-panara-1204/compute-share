[project]
name = "docker-compute-share"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "docker",
    "fastapi",
    "uvicorn[standard]",
    "psutil",
]
